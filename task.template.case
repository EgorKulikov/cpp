#include "../lib/io/input.h"
#include "../lib/io/output.h"

class %ClassName% {
public:
    void solve() {
        static int id = 1;
        err.printLine("Test", id);

        out.printLine("Case #" + to_string(id++) + ":");
    }
};
